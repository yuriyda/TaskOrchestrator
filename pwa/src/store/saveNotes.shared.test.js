// Re-imports shared/core/saveNotes.test.js so the compliance suite runs
// under the PWA vitest project too. The test file itself is storage-agnostic.
import '../../../shared/core/saveNotes.test.js'
